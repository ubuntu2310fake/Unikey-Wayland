--- wayland-client/src/main.cpp
+++ wayland-client/src/main.cpp
@@ -25,7 +25,9 @@
 
 // Evdev keycodes map
 static char get_ascii_from_keycode(uint32_t key, uint32_t mods) {
-    bool shift = (mods & 1); // Simplistic shift check
+    bool shift = (mods & 1) != 0; 
+    bool caps = (mods & 2) != 0;
+    bool uppercase = shift ^ caps;
 
     if (key >= 16 && key <= 25) {
         const char* row1 = "qwertyuiop";
-        return shift ? (row1[key-16] - 32) : row1[key-16];
+        return uppercase ? (row1[key-16] - 32) : row1[key-16];
     }
     if (key >= 30 && key <= 38) {
         const char* row2 = "asdfghjkl";
-        return shift ? (row2[key-30] - 32) : row2[key-30];
+        return uppercase ? (row2[key-30] - 32) : row2[key-30];
     }
     if (key >= 44 && key <= 50) {
         const char* row3 = "zxcvbnm";
-        return shift ? (row3[key-44] - 32) : row3[key-44];
+        return uppercase ? (row3[key-44] - 32) : row3[key-44];
     }
 
     if (key == 57) return ' '; // Space
